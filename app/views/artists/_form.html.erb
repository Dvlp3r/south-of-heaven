<%= form_for(@artist) do |f| %>
  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% @artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  Name: <br> <%= f.text_field :name %><br />
  Bio: <br> <%= f.text_area :bio %><br />
  Imgur Profile Pic URL: <br> <%= f. text_area :pic %><br />
  <%= link_to "Image Upload Link", "http://imgur.com/", target: "_blank", class: "btn btn-success btn-sm" %><br>
  Instagram Tag: <br> <%= f.text_field :instagramtag %><br />
<br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
